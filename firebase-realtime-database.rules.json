{
  "rules": {
    ".read": false,
    ".write": false,

    "users": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || data.child('role').val() === 'faculty' || data.child('role').val() === 'super_admin')",
        ".write": "auth != null && (auth.uid === $uid || root.child('users/' + auth.uid + '/role').val() === 'super_admin')",
        ".validate": "newData.hasChildren(['email','name','role']) && newData.child('email').isString() && newData.child('name').isString() && (newData.child('role').val() === 'student' || newData.child('role').val() === 'faculty' || newData.child('role').val() === 'super_admin') && (data.exists() ? (root.child('users/' + auth.uid + '/role').val() === 'super_admin' || newData.child('role').val() === data.child('role').val()) : (root.child('users/' + auth.uid + '/role').val() === 'super_admin' || (auth.uid === $uid && newData.child('role').val() === 'student') || (auth.uid === $uid && newData.child('role').val() === 'super_admin' && newData.child('email').val() === 'admin@college.edu')))"
      }
    },

    "facultyStatus": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth != null && ((auth.uid === $uid && (root.child('users/' + auth.uid + '/role').val() === 'faculty' || root.child('users/' + auth.uid + '/role').val() === 'super_admin')) || root.child('users/' + auth.uid + '/role').val() === 'super_admin')",
        ".validate": "newData.hasChildren(['facultyId','name','status']) && newData.child('facultyId').val() === $uid && newData.child('name').isString() && (newData.child('status').val() === 'Available' || newData.child('status').val() === 'Busy' || newData.child('status').val() === 'Leave')"
      }
    }
  }
}
